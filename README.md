# parser-exchange-rate-to-excel
Parser exchange rate to excel (NodeJs)

# Instruction

Clone project and run command:
`cd parser-exchange-rate-to-excel`

And install packages (first time)
`npm install`

Start project
`npm run start`
